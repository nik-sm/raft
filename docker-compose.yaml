version: "3.5"

services:
  raft-agent-0: 
    image: raft-image
    container_name: r0
    restart: "no"
    environment:
      - CONTAINER_NAME=r0
    build:
      context: .
    networks:
      - raft-network
    command: "-h=hostfile.json -t=${PRJ2_TEST_CASE} -d=${PRJ2_DURATION} -v=${PRJ2_VERBOSE}"

  raft-agent-1: 
    image: raft-image
    container_name: r1
    restart: "no"
    environment:
      - CONTAINER_NAME=r1
    build:
      context: .
    networks:
      - raft-network
    command: "-h=hostfile.json -t=${PRJ2_TEST_CASE} -d=${PRJ2_DURATION} -v=${PRJ2_VERBOSE}"

  raft-agent-2: 
    image: raft-image
    container_name: r2
    restart: "no"
    environment:
      - CONTAINER_NAME=r2
    build:
      context: .
    networks:
      - raft-network
    command: "-h=hostfile.json -t=${PRJ2_TEST_CASE} -d=${PRJ2_DURATION} -v=${PRJ2_VERBOSE}"

  raft-client-0:
    image: raft-image
    container_name: c0
    restart: "no"
    environment:
      - CONTAINER_NAME=c0
    build:
      context: .
    networks:
      - raft-network
    entrypoint: "client"
    command: "-hostfile=hostfile.json -data=datafile.txt -v=${PRJ2_VERBOSE}"
      

networks:
  raft-network:
    name: raft-network
    driver: bridge
